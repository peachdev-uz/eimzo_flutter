<?xml version="1.0" encoding="utf-8"?>
<!-- One numbered step in the QR guide (3e): a 24dp accent bullet, then the
     instruction with the thing to look for in semibold. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="13dp"
    android:orientation="horizontal">

    <TextView
        android:id="@+id/tvNumber"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:background="@drawable/eimzo_ui_bg_step_number"
        android:gravity="center"
        android:textColor="@color/eimzo_ui_accent_dark"
        android:textSize="12sp"
        android:textStyle="bold"
        tools:text="1" />

    <TextView
        android:id="@+id/tvStep"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:layout_weight="1"
        android:lineSpacingExtra="3dp"
        android:textColor="@color/eimzo_ui_hash_base"
        android:textSize="13.5sp"
        tools:text="Kompyuterda E-IMZO dasturini oching" />
</LinearLayout>
