<?xml version="1.0" encoding="utf-8"?>
<!-- An add-key method row (3c). White in light, the raised surface in dark —
     a card is a step above the page in both, which is what makes it a card. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/eimzo_ui_option_row" />
    <corners android:radius="18dp" />
</shape>
