<?xml version="1.0" encoding="utf-8"?>
<!-- A content card on the sign-request screen (3f). Radius 20 — larger than
     the 18 used for grouped rows, because these cards each hold one thing. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/eimzo_ui_option_row" />
    <corners android:radius="20dp" />
</shape>
